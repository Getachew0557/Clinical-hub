{
  "name": "backend",
  "version": "1.0.0",
  "description": "All backend microservices",
  "private": true,
  "scripts": {
    "dev": "concurrently -n \"GATEWAY,AUTH,PATIENT,APPT,DOCTOR,AI\" -c \"cyan,magenta,blue,green,white,greenBright\" \"npm run dev --prefix api-gateway\" \"npm run dev --prefix auth-service\" \"npm run dev --prefix patient-service\" \"npm run dev --prefix appointment-service\" \"npm run dev --prefix doctor-service\" \"npm run dev --prefix ai-service\"",
    "start": "concurrently -n \"GATEWAY,AUTH,PATIENT,APPT,DOCTOR,AI\" -c \"cyan,magenta,blue,green,white,greenBright\" \"npm start --prefix api-gateway\" \"npm start --prefix auth-service\" \"npm start --prefix patient-service\" \"npm start --prefix appointment-service\" \"npm start --prefix doctor-service\" \"npm start --prefix ai-service\"",
    "install:all": "for /d %d in (*) do if exist \"%d/package.json\" npm install --prefix %d"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
